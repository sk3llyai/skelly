# Skelly API — local environment (copy to .env and fill in)
# Port the API listens on locally.
PORT=3001

# DATABASE_URL is added at the Prisma / Postgres step (foundations).
# DATABASE_URL="postgresql://skelly:skelly@localhost:5432/skelly?schema=public"
