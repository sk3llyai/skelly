# @skelly/ui — RESERVED SEAM

The shared design system and reusable UI components (the view-builder, tables, dashboards,
pack layouts) used across every web surface.

**Status:** empty placeholder. Populated from Phase 3 onward as modules need shared components.
Kept as its own package so the design system is reusable and consistent (Living Platform Model,
Doc 15).
