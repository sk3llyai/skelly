# @skelly/calc-kernel — RESERVED SEAM

The central calculation engine: the tri-series (Value / Cash Flow / P&L) engine,
phases, the adjustments pipeline, the model dependency DAG, and the six core models.

**Status:** empty placeholder. Built in **Phase 2** (the ⭐ phase). It lives as its own
package from day one so the calculation core stays decoupled from the API and gets the
heaviest, most isolated test suite (financial correctness is the product).

See `docs/10-Module-Models-and-Templates.md` and `docs/21-Cost-Revenue-Product-Interaction-Model.md`.
