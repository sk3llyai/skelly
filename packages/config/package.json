{
  "name": "@skelly/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript / lint / test configuration for the Skelly monorepo",
  "files": [
    "tsconfig.base.json"
  ]
}
